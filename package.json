{
  "name": "microservice-config",
  "version": "1.0.0",
  "description": "Combines the command line, environment variables, a json file, and a default object into a single deep configuration object.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "env-obj": "0.0.2",
    "lodash": "^4.17.4",
    "subarg": "^1.0.0"
  }
}
